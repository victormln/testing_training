{% extends 'base.html.twig' %}

{% block body %}
    <h1>Members ({{ users|length }})</h1>
    <ul>
        {% for user in users %}
            <li><img src="{{ user.imageUrl }}" width="40px" height="40px"> {{ user.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}